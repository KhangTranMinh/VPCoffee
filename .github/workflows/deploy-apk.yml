# ============================================================================
# DISABLED WORKFLOW - NOT NEEDED
# ============================================================================
#
# This workflow is DISABLED because:
#
# 1. Fastlane handles deployments:
#    - Version increment
#    - APK build
#    - Firebase App Distribution
#    - Git commit and push
#
# 2. GitHub Pages auto-deploys on push to main:
#    - Configured in GitHub repo settings
#    - No workflow needed
#
# To deploy, use Fastlane:
#   bundle exec fastlane deploy_vpcoffee
#   bundle exec fastlane deploy_pushtest
#   bundle exec fastlane deploy_all
#
# This file is kept for reference only.
# ============================================================================

name: "[DISABLED] Deploy APK to GitHub Pages"

# DISABLED: No triggers
# Use Fastlane for deployments
on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    if: false  # Always skip
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is disabled. Use Fastlane instead."
